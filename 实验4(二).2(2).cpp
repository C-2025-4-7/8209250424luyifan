#include<iostream>
using namespace std;
int parseHex(const char* const hexString)
{
	int a,b;
	a = strlen(hexString);
	for (int i = 0; i <= a - 1; i++)
	{

		for (int j = 0; j <= i; j++)
		{
			b=a[i]*
		}
	}
}